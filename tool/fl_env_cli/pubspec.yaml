name: fl_env_cli
description: CLI tool for fl_env — encrypts .env files into native binary registries.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0
  crypto: ^3.0.0
  path: ^1.9.0
  pointycastle: ^3.9.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
